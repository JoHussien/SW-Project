import React, { Component } from 'react';

export default class insertingRates extends Component {
    state = {
        trips: false,

    }
        
 //insert a rate inside the database
    render() {

        return (
            <div>
                {/*html code */}
                {/* the add the 5 stars, description */}
                {/* then call the function to insert these data into the database */}
                    
            </div >
        )
    }

}